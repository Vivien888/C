答：
（1）对于 string 类型的变量来说，因为 string 类型本身接受无参数的初始化方式，
所以不论变量定义在函数内还是函数外都被默认初始化为空字符串。

（2）对于内置类型 int 来说，变量 global_int 定义在所有函数体之外，
根据 C++ 的规定，global_int 默认初始化为 0，而变量 local_int 定义在 main 函数的内部，将不被初始化，
如果程序试图拷贝或输出未初始化的变量，将遇到一个为定义的奇异值。
