答：
(a) 是非法的，cp 是一个常量指针，因其值不能被改变，所以必须初始化。

(b) 是非法的，p2 是一个常量指针，因其值不能被改变，所以必须初始化。

(c) 是非法的，ic 是一个常量，因其值不能被改变，所以必须初始化。

(d) 是非法的，p3 是一个常量指针，因其值不能被改变，所以必须初始化；
     同时 p3 指向的是常量，即不能通过 p3 改变所指对象的值。

(e) 是合法的，但是 p 没有指向任何实际的对象。
