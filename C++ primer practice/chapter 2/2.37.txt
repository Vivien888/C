答：
（1）decltype(a) c=a; 使用的是一个不加括号的变量，因此 c 的类型就是 a 的类型；
         所以c 的类型是 int，值为3；
（2）decltype(a = b) d = a;编译器分析表达式a=b并得到它的类型作为 d 的推断类型，但是不实际计算该表达式，
         所以 整型变量a 的值不发生改变，仍然是3；
         d 的类型是 int&，d 是 a 的别名，值是3；
         整型变量b 的值一直没有发生改变，为4。
