答：
（1）前置递增运算符首先将运算对象加1（或减1），然后把改变后的对象作为求值结果，将对象本身作为左值返回；  
         前置递增运算符避免了不必要的工作，它把值加1后直接返回改变了的运算对象。
（2）后置递增运算符也将运算对象加1（或减1），但是求值结果是运算对象改变之前那个值的副本，将对象原始值的副本作为右值返回。
         后置递增运算符需要将原始值存储下来以便于这个未修改的内容，如果我们不需要修改之前的值，则是一种浪费。
