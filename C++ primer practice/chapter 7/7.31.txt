答；
class X;
class Y{
    X* x;	
};
class X{
    Y y;
};

PS：可以定义指向不完全类型的指针，但是无法创建不完全类型的对象。
即可以先定义类 X 的声明称为前向声明，它向程序中引入了名字 X 并且指明 X 是一种类类型。
对于类型 X 来说，此时我们已知它是一个类类型，但是不清楚它到底包含哪些成员，所以它是一个不完全类型。
但不可以先定义类Y，如下代码错误：
class Y;
class X{
    Y y;	
};
class Y{
    X* x;
};