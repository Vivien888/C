答：
我们之前定义的 Person 类含有3个构造函数：
（1）前两个构造函数接受的参数个数都不是1，所以不存在隐式转换的问题，也就不必指定 explicit。
（2）最后一个构造函数 Person(std::istream &is); 只接受一个参数，默认情况下它会把读入的数据自动转换成 Person 对象。
如果确实需要使用 Person 对象，可以明确指定；
在其他情况下则不希望自动类型转换的发生。所以应该把这个构造函数指定为 explicit 的。
