答：
第一条、第二条和第四条语句是合法的；

在第三条语句不合法：
第一个双引号被注释掉了，因此 << 运算符后真正被编译的内容是 " */，编译器认为这是一个不完整的字符串。
运行报错：7 error：missing terminating " character，即“缺少结束的双引号”。
改正方法：在分号前补一个双引号
                 std::cout << /* "*/" */";

